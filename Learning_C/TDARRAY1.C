/* a two dimensional array is loaded with the numbner 1 though 12. */

#include "stdio.h"

 main()
  {
   int t,i, num[3][4];

   for(t-0; t<3; ++t)
     for(i=0; i<4; ++i)
       num[t][i] = (t*4)+i+1;
}
